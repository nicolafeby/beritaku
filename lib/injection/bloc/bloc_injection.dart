part of '../injection.dart';

class BlocInjection {
  static Future<void> configureBlocInjection() async {
    // sl.registerFactory(() => MainMenuBloc(usecase: sl()));
    // sl.registerFactory(() => PokemonDetailBloc(usecase: sl()));
  }
}
